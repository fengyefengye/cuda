**结论先看**
这组结果非常清晰：在你当前机器与环境下，图模式路径和融合路径都比 Eager 快，但“图模式（当前实际是 TorchScript）”整体略优于自写融合路径。

数据来源：summary.md, summary.json

**核心对比**
1. Batch=1  
eager 2.3885ms，graph 1.9205ms，fused 2.0572ms  
graph 相对 eager 提升约 24.4%，fused 提升约 16.1%

2. Batch=32  
eager 2.3870ms，graph 1.7700ms，fused 1.7976ms  
graph 提升约 34.8%，fused 提升约 32.8%

3. Batch=128  
eager 2.4192ms，graph 1.9098ms，fused 2.0362ms  
graph 提升约 26.7%，fused 提升约 18.8%

**怎么解读**
1. 你的实验目标“动态图碎片化 vs 图优化/融合”被验证了。  
三种模式下，中位延迟差异稳定存在，Eager 始终最慢。

2. 当前最优不是自写 fused，而是 graph 路径。  
这并不矛盾，因为你只融合了第一层（Conv+ReLU+Pool），其余层仍是普通算子；而图模式可在更大范围做调度优化。

3. fused 在显存峰值上有明显优势（中大 batch 更明显）。  
batch=128 时，fused 峰值显存 11.51MB，对比 eager/graph 的 13.96MB，降低约 17.5%。  
这和“减少中间张量落地”的理论一致。

4. 尾延迟方面：  
batch=32 时 fused 的 p95 略好于 graph（2.3043 vs 2.3537），说明 fused 路径在部分负载下稳定性不错；但整体中位数仍是 graph 更优。

**一个关键前提（很重要）**
你的 graph 路径在本环境不是 torch.compile 真正运行，而是回退到了 TorchScript。  
可见 summary.json 中 build_info 字段：
- compile_backend_used = torch.jit.script
- compile_error = Windows not yet supported for torch.compile

所以这轮结论应准确表述为：
Eager vs TorchScript 图模式 vs 自写 CUDA 融合。

**可信度与风险**
1. 可信度高的部分  
- 同一模型同一权重对比  
- 有 warmup 与同步计时  
- correctness 通过（误差在阈值内）

2. 还可加强的部分  
- 目前只有一轮主实验，建议至少重复 3 轮取中位数的中位数  
- 还没有 kernel 级证据（例如 kernel 数量、launch 次数），建议补 nsys

**一句话总结**
你的结果已经支持最初观点：Eager 更慢，图模式/融合更快；在这台机器上，图模式（TorchScript）是速度最优，自写融合带来显存优势且有明显加速，但还没超过图模式的总体收益。  

已完成结果解读，给出了量化结论、约束条件和可解释原因。继续使用 Autopilot已完成对基准结果的定量分析：给出了三种执行路径在 batch 1/32/128 下的速度对比、加速比例、显存差异、尾延迟表现，并明确了当前图模式实际为 TorchScript 回退这一前提及其对结论边界的影响。继续使用 Autopilot本轮工作已全部收尾：完成了项目实现、在 hamer 环境下完成三路基准运行，并对结果做了结构化分析与结论说明。

